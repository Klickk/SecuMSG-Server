# who/what to pull
OWNER=Klickk
REPO=Sem7Individual
# tag to pull (use sha-<commit> for immutable deploys, or latest)
TAG=latest

# secrets (SET THESE ON THE SERVER):
POSTGRES_PASSWORD=change-me
SIGNING_KEY=change-me-super-long-random-secret

# cors
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
